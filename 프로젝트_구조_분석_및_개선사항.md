# MeeTeam Backend 프로젝트 구조 분석 및 개선 제안

> 작성일: 2026-02-07
> 분석 대상: meeteam-backend (Spring Boot)

---

## 목차

1. [현재 패키지 구조 개요](#1-현재-패키지-구조-개요)
2. [통일성 부족 문제](#2-통일성-부족-문제)
3. [가독성이 떨어지는 클래스명](#3-가독성이-떨어지는-클래스명)
4. [불필요하거나 중복된 클래스](#4-불필요하거나-중복된-클래스)
5. [패키지 구조 아쉬운 점](#5-패키지-구조-아쉬운-점)
6. [개선 제안](#6-개선-제안)
7. [우선순위별 액션 아이템](#7-우선순위별-액션-아이템)

---

## 1. 현재 패키지 구조 개요

```
src/main/java/com/wardk/meeteam_backend/
├── domain/          # 도메인 레이어 (비즈니스 로직)
│   ├── applicant/
│   ├── chat/
│   ├── codereview/
│   ├── file/
│   ├── job/         🆕 신규 추가
│   ├── llm/
│   ├── member/
│   ├── notification/
│   ├── pr/
│   ├── project/
│   ├── projectlike/     # ✅ 소문자로 변경됨
│   ├── projectmember/   # ✅ 소문자로 변경됨
│   ├── review/
│   ├── skill/
│   └── webhook/
├── web/             # 프레젠테이션 레이어 (Controller + DTO)
│   ├── auth/
│   ├── codereview/
│   ├── file/
│   ├── mainpage/
│   ├── member/
│   ├── notification/
│   ├── pr/
│   ├── project/
│   ├── projectlike/     # ✅ 소문자로 변경됨
│   ├── projectmember/   # ✅ 소문자로 변경됨
│   ├── recruitmentState/
│   └── review/
└── global/          # 공통 모듈 (설정, 인증, 예외처리)
    ├── auth/
    ├── config/
    ├── github/
    ├── response/
    └── support/
```

### 현재 통계

| 항목 | 개수 |
|------|------|
| 총 Java 클래스 | 261개 |
| 도메인 모듈 | 13개 |
| 컨트롤러 | 14개 |
| 서비스 (인터페이스 + 구현체) | 26개 |
| 리포지토리 | 28개 |
| DTO | 79개 |
| 엔티티 | 33개 |

---

## 2. 통일성 부족 문제

### 2.1 Service 인터페이스 패턴 불일치 🔴

**Interface → Impl 패턴을 따르는 서비스 (8개)**
```
✅ ProjectService → ProjectServiceImpl
✅ MemberProfileService → MemberProfileServiceImpl
✅ ProjectMemberService → ProjectMemberServiceImpl
✅ ProjectApplicationService → ProjectApplicationServiceImpl
✅ ReviewService → ReviewServiceImpl
✅ PullRequestService → PullRequestServiceImpl
✅ PullRequestIngestionService → PullRequestIngestionServiceImpl
✅ LlmOrchestrator → LlmOrchestratorImpl
```

**인터페이스 없이 직접 구현된 서비스 (10개+)**
```
❌ GithubWebhookService (인터페이스 없음)
❌ LlmReviewService
❌ LlmSummaryService
❌ S3FileService
❌ NotificationService
❌ NotificationSubscribeService
❌ SSENotificationService
❌ PrReviewService
❌ PrReviewJobService
❌ PrReviewOrchestrationService
❌ ProjectLikeService
❌ AuthService
❌ GithubAppAuthService
```

> **문제점**: 테스트 시 Mock 처리가 어렵고, 확장성이 떨어짐

---

### 2.2 DTO 배치 위치 불일치 🟡

**대부분의 DTO**: `web/` 레이어에 위치 (77개) ✅
**일부 DTO**: `domain/` 레이어에 위치 (2개) ❌

```
❌ domain/file/dto/FileListResponse.java
❌ domain/file/dto/FileUploadResponse.java
```

> **문제점**: 도메인 레이어에서 `web/` 레이어의 DTO를 참조하는 레이어 위반 발생

예시:
```java
// domain/member/service/MemberProfileService.java
public interface MemberProfileService {
    MemberProfileResponse profile(Long memberId);  // ← web 레이어 import
}
```

---

### 2.3 DTO 디렉토리 구조 불일치 🟡

**신규 구조 (request/response 분리)**
```
web/member/dto/
├── request/
│   ├── MemberProfileUpdateRequest.java
│   └── MemberSearchRequest.java
└── response/
    ├── MemberCardResponse.java
    ├── MemberProfileResponse.java
    └── ...
```

**구 구조 (혼재)**
```
web/auth/dto/
├── login/
├── oauth/
└── register/  ← request/response 분리 안됨
```

> **문제점**: auth 모듈만 다른 디렉토리 구조 사용

---

## 3. 가독성이 떨어지는 클래스명

### 3.1 너무 일반적인 이름 🔴

| 현재 이름 | 문제점 | 제안 |
|-----------|--------|------|
| `FileObject.java` | 무엇을 나타내는지 불명확 | `FileMetadata.java` 또는 `UploadedFileInfo.java` |
| `FileUtil.java` | 너무 범용적 | `S3FileUploadUtil.java` 또는 `FileValidationUtil.java` |
| `Payload.java` | 의미 불명확 | `NotificationPayload.java` 또는 구체적 용도명 |
| `TechStack.java` (enum) | 애매함 | `Technology.java` 또는 `TechType.java` |

### 3.2 약어 사용으로 인한 가독성 저하 🟡

| 현재 이름 | 제안 |
|-----------|------|
| `PrData.java` | `PullRequestData.java` |
| `PrFileData.java` | `PullRequestFileData.java` |
| `PrReviewJob.java` | `PullRequestReviewJob.java` |
| `PrReviewFinding.java` | `PullRequestReviewFinding.java` |

### 3.3 오타 또는 잘못된 네이밍 🔴

| 현재 이름 | 문제점 | 제안 |
|-----------|--------|------|
| `SeedSevice.java` | 오타 (Service → Sevice) | `SeedService.java` ❌ (삭제됨) |

---

## 4. 불필요하거나 중복된 클래스

### 4.1 중복된 클래스명 (컴파일 에러 위험) ✅ 해결됨

| 기존 클래스명 | 변경 전 위치 | 변경 후 |
|---------------|--------------|---------|
| `ProjectResponse.java` | `web/member/dto/response/` | ✅ `MemberProjectResponse.java`로 변경 |
| `ReviewResponse.java` | `web/member/dto/response/` | ✅ `MemberReviewResponse.java`로 변경 |
| `SubCategoryRequest.java` | `web/auth/dto/register/` | ✅ `JobPositionRequest.java`로 변경 |
| `SubCategoryRequest.java` | `web/project/dto/request/` | ✅ 삭제 (`ProjectRecruitRequest` 사용)

### 4.2 삭제된 클래스 (현재 브랜치에서 제거됨) ✅

```
✅ BigCategory.java (삭제됨)
✅ SubCategory.java (삭제됨)
✅ MemberSubCategory.java (삭제됨)
✅ MemberSubCategoryRepository.java (삭제됨)
✅ SubCategoryRepository.java (삭제됨)
✅ ProjectMemberApplication.java (ProjectApplication으로 대체)
✅ SeedController.java (삭제됨)
✅ SeedService.java (삭제됨)
✅ TestController.java (삭제됨)
```

### 4.3 사용 여부 확인 필요한 클래스 🟡

| 클래스 | 위치 | 확인 필요 사항 |
|--------|------|----------------|
| `WhitelistEntry.java` | global/support | 실제 사용처 확인 |
| `OAuthCodeRepository.java` | global/auth | Redis 또는 DB 사용 확인 |
| `TokenBlacklistRepository.java` | global/auth | 로그아웃 처리 시 사용 확인 |

---

## 5. 패키지 구조 아쉬운 점

### 5.1 도메인 간 의존성 문제 🟡

```
projectMember → project (O)
projectMember → member (O)
project → skill (O)
notification → project, member (O)

❌ domain/member/service → web/member/dto (레이어 위반)
❌ domain/project/service → web/project/dto (레이어 위반)
```

### 5.2 DTO 폴더 위치의 혼란 🟡

**현재 구조**:
- Controller와 DTO가 같은 모듈에 있음 ✅
- 하지만 Service 인터페이스에서 web 레이어의 DTO 참조 ❌

**권장 구조**:
```
domain/member/
├── dto/              # 서비스에서 사용하는 Command/Query DTO
│   ├── MemberProfileCommand.java
│   └── MemberSearchQuery.java
├── entity/
├── repository/
└── service/

web/member/
├── controller/
└── dto/              # Request/Response DTO (Controller용)
    ├── request/
    └── response/
```

### 5.3 공통 DTO 위치 부재 🟡

여러 도메인에서 공유하는 DTO가 없음:
- `PageResponse<T>` - 페이징 공통 응답
- `SliceResponse<T>` - 무한스크롤 공통 응답

> **제안**: `global/dto/` 또는 `web/common/dto/` 패키지 생성

### 5.4 Enum 위치 분산 🟡

Enum들이 각 도메인 entity 패키지에 흩어져 있음:
```
domain/applicant/entity/ApplicationStatus.java
domain/member/entity/Gender.java
domain/project/entity/ProjectStatus.java
domain/notification/entity/NotificationType.java
```

> **대안**: 규모가 커지면 `domain/common/enums/` 또는 각 도메인 내 `enums/` 패키지로 분리

---

## 6. 개선 제안

### 6.1 Service 인터페이스 패턴 통일 (권장)

```java
// Before: 인터페이스 없이 직접 구현
@Service
public class ProjectLikeService { ... }

// After: 인터페이스 분리
public interface ProjectLikeService { ... }

@Service
public class ProjectLikeServiceImpl implements ProjectLikeService { ... }
```

**장점**:
- 단위 테스트 시 Mock 처리 용이
- 의존성 역전 원칙(DIP) 준수
- 확장 및 교체 용이

### 6.2 DTO 레이어 분리 (권장)

```
# 서비스 레이어용 Command/Query 패턴
domain/{module}/dto/
├── command/
│   ├── CreateProjectCommand.java
│   └── UpdateProjectCommand.java
└── query/
    └── ProjectSearchQuery.java

# 프레젠테이션 레이어용 Request/Response
web/{module}/dto/
├── request/
│   └── ProjectPostRequest.java  # → Command로 변환
└── response/
    └── ProjectResponse.java     # ← Query 결과로부터 변환
```

### 6.3 공통 응답 클래스 추가 (권장)

```java
// global/dto/PageResponse.java
public record PageResponse<T>(
    List<T> content,
    int page,
    int size,
    long totalElements,
    int totalPages,
    boolean hasNext
) { }

// global/dto/SliceResponse.java
public record SliceResponse<T>(
    List<T> content,
    int size,
    boolean hasNext
) { }
```

### 6.4 Fetcher 패턴 통일 (권장)

현재 `PullRequestFetcher`가 전략 패턴으로 구현됨:
```
domain/pr/service/fetcher/
├── PullRequestFetcher.java (interface)
├── GithubApiFetcher.java
└── MockFetcher.java
```

다른 외부 API 연동에도 동일한 패턴 적용 권장.

### 6.5 패키지명 통일 ✅ 해결됨

| 변경 전 | 변경 후 | 상태 |
|---------|---------|------|
| `projectMember` | `projectmember` | ✅ 완료 |
| `projectLike` | `projectlike` | ✅ 완료 |
| `codereview` | `codereview` | 유지 (이미 소문자) |

> **참고**: Java 패키지명 컨벤션은 소문자 권장

---

## 7. 우선순위별 액션 아이템

### 🔴 높음 (즉시 수정 권장)

| # | 항목 | 작업 내용 | 상태 |
|---|------|----------|------|
| 1 | 중복 클래스명 해결 | `ProjectResponse`, `ReviewResponse`, `SubCategoryRequest` 이름 변경 | ✅ 완료 |
| 2 | 패키지명 소문자 변경 | `projectMember`, `projectLike` → 소문자로 변경 | ✅ 완료 |
| 3 | 레이어 위반 수정 | Service 인터페이스에서 web DTO 참조 제거 | 미완료 |

### 🟡 중간 (리팩토링 시 적용)

| # | 항목 | 작업 내용 |
|---|------|----------|
| 3 | Service 인터페이스 통일 | 인터페이스 없는 Service에 인터페이스 추가 |
| 4 | DTO 디렉토리 구조 통일 | auth 모듈도 request/response 분리 |
| 5 | 공통 DTO 추가 | `PageResponse`, `SliceResponse` 등 |
| 6 | 가독성 떨어지는 클래스명 변경 | `FileObject` → `FileMetadata` 등 |

### 🟢 낮음 (향후 개선)

| # | 항목 | 작업 내용 |
|---|------|----------|
| 7 | Command/Query 패턴 도입 | 서비스 레이어용 DTO 분리 |
| 8 | Enum 패키지 분리 | 규모 커지면 별도 패키지로 |

---

## 부록: 권장 패키지 구조 (미래)

```
com.wardk.meeteam_backend/
├── domain/
│   ├── common/
│   │   ├── dto/          # 공통 Command/Query
│   │   └── entity/       # BaseEntity 등
│   ├── member/
│   │   ├── dto/
│   │   │   ├── command/  # CreateMemberCommand, UpdateMemberCommand
│   │   │   └── query/    # MemberSearchQuery
│   │   ├── entity/
│   │   ├── repository/
│   │   └── service/
│   │       ├── MemberService.java (interface)
│   │       └── MemberServiceImpl.java
│   └── project/
│       └── ... (동일 구조)
├── web/
│   ├── common/
│   │   └── dto/          # PageResponse, SliceResponse
│   ├── member/
│   │   ├── controller/
│   │   └── dto/
│   │       ├── request/  # Web 요청 DTO
│   │       └── response/ # Web 응답 DTO
│   └── project/
│       └── ... (동일 구조)
└── global/
    ├── auth/
    ├── config/
    ├── exception/
    └── response/
```

---

## 마무리

현재 MeeTeam Backend는 전반적으로 잘 구조화되어 있으나, 위에서 언급한 **통일성 문제**와 **중복 클래스** 이슈를 해결하면 더욱 유지보수하기 좋은 코드베이스가 될 것입니다.

특히 **중복 클래스명 문제**는 IDE에서 import 혼동을 일으킬 수 있으므로 가장 먼저 해결하는 것을 권장합니다.
